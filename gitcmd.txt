-----basic---------

git --version
git config --global user.name  "  "
git config --global user.email "  "
git --list
git help config
git config --help
git init
ls -la
rm -rf .git
git status
touch .gitignore


----adding --------


git add -A    ///---all files
git add filename
git reset filename
git commit -m "  initial"
git log
git clone <url>  <where to clone>
git clone ../remote_repo
git remote -v
git branch -a
---------commit changes--------
git diff
git status
git add -A
git commit -m "modify"

----push---
git pull origin master
git push origin master
git remote add  <gitrepo> http://
git oush -u  <gitrepo>


-----branch----
git branch <branchname>
git checkout <branchname created> -- to switch to that branch
git push -u origin <branchname>
git branch -a
---------merge branch--------
git checkout master
git  pull origin master
git branch --merged
git merge <branchname>
git push origin master

----deleting branch-------
git branch --merged
git branch -d <branchname>
git branch -a
git push origin --delete <branchname>

-----log-------
git log
git log --author="sinzith"

git reset HEAD <filename>


 





